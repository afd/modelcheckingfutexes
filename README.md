# modelcheckingfutexes

Promela models of futex-based synchronisation primitives.

The relevant Promela code is in the `promela` subdirectory.

C++ samples based on code from Drepper's "Futexes Are Tricky" paper is
under the `cpp` subdirectory.
